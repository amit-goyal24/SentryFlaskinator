I am completeing this project by running these below cmd whcih help you to install all the nessasary thing which is installed in your vs code for run.

python -m venv venv

.\venv\Scripts\Activate.ps1

pip install "sentry-sdk[flask]"

pip install -r requirements.txt

python app.py

 At last  I have to run "app.py"  then Go  to the server "http://localhost:5000/err"  ..
and also go "http://localhost:5000/ "  
then your code will run and show the suitable aoutput in  browser
